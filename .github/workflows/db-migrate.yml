name: Migrate Cloud SQL (Drizzle)

# Runs two ways:
#
#   workflow_call  - deploy-cloudrun.yml calls this BEFORE deploying the container,
#                    so code never reaches Cloud Run ahead of the schema it needs.
#                    `drizzle-kit migrate` is idempotent (it reads
#                    drizzle.__drizzle_migrations and applies only what is pending),
#                    so running it on every deploy costs seconds when nothing changed.
#   workflow_dispatch - manual run, for applying a migration without shipping code.
#
# The `production` environment below is the safety gate: give it required reviewers
# in Settings > Environments and every schema change waits for a human Approve,
# whether it arrived via deploy or by hand.
on:
  workflow_call:
    inputs:
      command:
        description: 'Drizzle command to run'
        required: false
        default: 'migrate'
        type: string
  workflow_dispatch:
    inputs:
      command:
        description: 'Drizzle command to run'
        required: true
        default: 'migrate'
        type: choice
        options:
          - migrate
          - push

permissions:
  contents: read
  id-token: write

concurrency:
  group: db-migrate
  cancel-in-progress: false

env:
  PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  # google-github-actions/auth infers a project from the service account email and
  # exports it as CLOUDSDK_CORE_PROJECT, which OUTRANKS `gcloud config set project`.
  # A default compute SA is <number>-compute@developer.gserviceaccount.com, so the
  # inferred project becomes the literal string "developer" and every gcloud call
  # in this job hits the wrong (nonexistent) project. Pinning it here — workflow
  # env beats anything the action writes to GITHUB_ENV — keeps the job on target.
  # deploy-cloudrun.yml has carried this same pin for a while; this workflow was
  # missing it, which is why the first automated migration failed on
  # `secretmanager.googleapis.com not enabled on project [developer]`.
  CLOUDSDK_CORE_PROJECT: ${{ vars.GCP_PROJECT_ID }}
  REGION: ${{ vars.GCP_REGION || 'asia-southeast1' }}
  CLOUD_SQL_INSTANCE: ${{ vars.CLOUD_SQL_INSTANCE }}
  SQL_DB_NAME: ${{ vars.SQL_DB_NAME || 'hnxcis' }}
  SQL_ADMIN_USER: ${{ vars.SQL_ADMIN_USER || 'postgres' }}
  DB_ADMIN_PASSWORD_SECRET: ${{ vars.DB_ADMIN_PASSWORD_SECRET || 'hnxcis-db-admin-password' }}
  WIF_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}

jobs:
  migrate:
    name: Apply migrations
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # `push` diffs schema.ts straight against the live database and never looks
      # at drizzle/, so it silently skips migrations 0001-0003: the 30 tables show
      # up, but RLS, the hard-delete guard, append-only audit and versioning do
      # not. That is survivable as a deliberate manual act on an empty dev DB; it
      # must never happen as a side effect of merging a pull request.
      - name: Refuse `push` on an automated run
        if: inputs.command == 'push' && github.event_name != 'workflow_dispatch'
        run: |
          echo '::error::drizzle-kit push is only allowed from a manual workflow_dispatch, not from an automated deploy. It bypasses migrations 0001-0003 and would drop RLS, the hard-delete guard, append-only audit and versioning.'
          exit 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Validate configuration
        run: |
          if [ -z "${PROJECT_ID:-}" ]; then
            echo "::error::GCP_PROJECT_ID (repository variable) is empty. Without it gcloud falls back to whatever project google-github-actions/auth infers from the service account email — for a default compute SA that is the literal string 'developer'."
            exit 1
          fi
          if [ -z "${CLOUD_SQL_INSTANCE:-}" ]; then
            echo "::error::CLOUD_SQL_INSTANCE (repository variable) is empty. Expected the full connection name: project:region:instance."
            exit 1
          fi

      - name: Authenticate to Google Cloud (Workload Identity Federation)
        if: env.WIF_PROVIDER != ''
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SA }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Authenticate to Google Cloud (service account key)
        if: env.WIF_PROVIDER == ''
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      # Fails here with an actionable message rather than midway through, after the
      # proxy is up and a half-applied migration is possible.
      - name: Preflight - confirm gcloud resolved the intended project
        run: |
          set -euo pipefail
          RESOLVED="$(gcloud config get-value project 2>/dev/null || true)"
          echo "Intended project : ${PROJECT_ID}"
          echo "Resolved project : ${RESOLVED}"
          if [ "${RESOLVED}" != "${PROJECT_ID}" ]; then
            echo "::error::gcloud resolved project '${RESOLVED}' instead of '${PROJECT_ID}'. CLOUDSDK_CORE_PROJECT (exported by google-github-actions/auth from the service account email) outranks 'gcloud config set project'."
            exit 1
          fi

      - name: Read DB admin password from Secret Manager
        id: secret
        run: |
          set -euo pipefail
          # --project is explicit on purpose: it does not rely on whatever the
          # resolved default happens to be at this point in the job.
          PASSWORD="$(gcloud secrets versions access latest \
            --secret="${DB_ADMIN_PASSWORD_SECRET}" \
            --project="${PROJECT_ID}")"
          echo "::add-mask::${PASSWORD}"
          echo "password=${PASSWORD}" >> "$GITHUB_OUTPUT"

      - name: Start Cloud SQL Auth Proxy
        run: |
          set -euo pipefail
          curl -fsSL -o cloud-sql-proxy \
            https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.14.1/cloud-sql-proxy.linux.amd64
          chmod +x cloud-sql-proxy
          ./cloud-sql-proxy --port 5432 "${CLOUD_SQL_INSTANCE}" > proxy.log 2>&1 &
          for i in $(seq 1 30); do
            if nc -z 127.0.0.1 5432; then
              echo "Cloud SQL Auth Proxy is ready"
              exit 0
            fi
            sleep 1
          done
          echo "::error::Cloud SQL Auth Proxy did not start"
          cat proxy.log
          exit 1

      - name: Run drizzle-kit ${{ inputs.command }}
        env:
          SQL_HOST: 127.0.0.1
          SQL_PORT: '5432'
          SQL_DB_NAME: ${{ env.SQL_DB_NAME }}
          SQL_ADMIN_USER: ${{ env.SQL_ADMIN_USER }}
          SQL_ADMIN_PASSWORD: ${{ steps.secret.outputs.password }}
          SQL_SSL: 'false'
        run: npm run db:${{ inputs.command }}

      - name: Summary
        run: |
          {
            echo "### Cloud SQL migration finished"
            echo ""
            echo "- Command: \`drizzle-kit ${{ inputs.command }}\`"
            echo "- Instance: \`${CLOUD_SQL_INSTANCE}\`"
            echo "- Database: \`${SQL_DB_NAME}\`"
          } >> "$GITHUB_STEP_SUMMARY"
